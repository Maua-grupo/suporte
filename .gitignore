./includes/config.inc.php

# Environment files (keep template versioned)
.env
.env.*
!.env.example

# Local runtime logs
includes/logs/*.txt

# OS/editor junk
**/__MACOSX/
.DS_Store
Thumbs.db
*.swp
*.swo

# Impeccable (design skill) — instalado/gerado; reproduza com `npx impeccable install`
# (PRODUCT.md e DESIGN.md ficam versionados de propósito: são contexto de design do time)
.claude/skills/impeccable/
.impeccable/